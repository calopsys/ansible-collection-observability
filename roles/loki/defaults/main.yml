---
# Automatically generated by aar-doc
# Remote path to the directory containing environment configs.
calopsys_loki_env_dir: /etc/default
# Remote path to the loki configurations directory.
calopsys_loki_config_dir: /etc/loki
# When using the standalone template, configure the log retention period. Defaults to 1 month.
calopsys_loki_standalone_retention_period: 365d
# Required when using the 'standalone_s3' template, specifies the log level for the loki daemon.
calopsys_loki_standalone_log_level: warn
