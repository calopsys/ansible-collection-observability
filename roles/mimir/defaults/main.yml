---
# Automatically generated by aar-doc
# Absolute path to the mimir configuration directory.
calopsys_mimir_config_dir: /etc/mimir
# Absolute path to the directory containing environment configurations.
calopsys_mimir_env_dir: /etc/default
# Required when using the 'standalone_s3' template, specifies the tenant name.
calopsys_mimir_standalone_tenant_name: anonymous
# Required when using the 'standalone_s3' template, specifies the log level for the mimir daemon.
calopsys_mimir_standalone_log_level: warn
